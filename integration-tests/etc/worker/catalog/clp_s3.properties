connector.name=clp
clp.storage-type=s3
clp.s3-auth-provider=clp_package
# No bucket set: the split path already starts with the bucket name, so the worker builds
# <endpoint>/fixtures/<table>/<archive>.
clp.s3-end-point=http://minio:9000
clp.s3-access-key-id=minioadmin
clp.s3-secret-access-key=minioadmin
