node.environment=test
# Pinned rather than the image default of ${ENV:HOSTNAME}, which changes identity on every
# container recreation.
node.id=clp-integration-test-coordinator
node.location=integration-test
node.internal-address=coordinator
node.data-dir=/var/lib/presto/data
