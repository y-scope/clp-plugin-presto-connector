# Values used by `ct lint` to render the S3 storage path.
archiveStorage:
  type: "s3"
  s3:
    bucket: "clp-archives"
    regionCode: "us-east-1"
    accessKeyId: "test-access-key"
    secretAccessKey: "test-secret-key"

metadataDb:
  host: "clp-database"
  username: "clp-user"
  password: "clp-password"
