yamale>=6.1.0

# Pinned exactly. The venv installs with `pip install --upgrade`, so a floor resolves to whatever
# PyPI serves that day, which turns an upstream release into a failure on whichever pull request
# happens to run next.
yamllint==1.38.0
