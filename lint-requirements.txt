# Pinned exactly. `ruff` and `mypy` are pinned by `integration-tests/uv.lock`, so this is the only
# linter that could otherwise float, and a floating linter turns an upstream release into a failure
# on whichever pull request happens to run next.
yamllint==1.38.0
